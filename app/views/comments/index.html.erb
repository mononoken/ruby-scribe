<%= link_to "Back to #{@note.name}", note_path(@note) %>

<%= turbo_frame_tag dom_id(@note, :comments), refresh: :morph do %>
  <div
    class="flex flex-col space-y-2"
  >
    <%= render @comments %>
  </div>
<% end %>

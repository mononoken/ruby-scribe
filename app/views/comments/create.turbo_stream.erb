<%# Prepend is performed by Turbo Stream channel in controller %>

<%= turbo_stream.replace dom_id(@note, :new_comment) do %>
  <%= turbo_frame_tag dom_id(@note, :new_comment) do %>
    <%= render "form", {note: @note, comment: @comment} %>
  <% end %>
<% end %>

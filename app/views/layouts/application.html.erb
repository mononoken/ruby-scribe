<!DOCTYPE html>
<html>
  <head>
    <title>Fae Scribe</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body class="bg-slate-800 text-indigo-300">
    <header
      class="min-h-80 sticky inset-0 flex w-full items-center 
             justify-between bg-slate-800 px-4 py-2"
    >
      <h1 class="text-3xl font-bold text-orange-300">
        <%= link_to "Fae Scribe", root_path %>
      </h1>
      <nav class="" data-controller="popover">
        <button
          class="inline"
          data-action="popover#popover"
          data-testid="nav-hamburger-btn"
        >
          <svg
            xmlns="http://www.w3.org/2000/svg"
            fill="none"
            viewBox="0 0 24 24"
            stroke-width="1.5"
            stroke="currentColor"
            class="h-8 w-8"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
            />
          </svg>
        </button>
        <div
          hidden
          class="fixed inset-0 h-full w-full z-40"
          data-popover-target="togglable"
        >
          <div
            class="fixed h-full w-full z-40 bg-slate-800/30 backdrop-blur-sm"
            data-action="click->popover#popover"
            aria-hidden="true"
          >
          </div>
          <div
            class="fixed right-[-80%] h-full w-[80%] z-50
                   flex flex-col justify-between
                   bg-slate-600 p-[24px] rounded-l-2xl"
            data-popover-target="slideable"
          >
            <div
              class="flex-none flex justify-between"
            >
              <h1
                class="flex-none text-2xl font-bold text-orange-300"
              >
                <%= link_to "Fae Scribe", root_path %>
              </h1>
              <button
                class="flex-none h-8 w-8"
                data-action="popover#popover"
              >
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke-width="1.5"
                  stroke="currentColor"
                  class="w-6 h-6"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M6 18L18 6M6 6l12 12"
                  />
                </svg>
              </button>
            </div>
            <ul
              class="flex-auto overflow-auto flex-vertical space-y-[4px] items-stretch text-base leading-4"
            >
              <li 
                class="relative bg-slate-700 px-[16px] py-[8px] rounded-md"
              >
                <%= link_to "About", about_path,
                  class: "text-purple-400 before:absolute before:inset-0" %>
              </li>
              <% if current_user %>
                <li 
                  class="relative bg-slate-700 px-[16px] py-[8px] rounded-md"
                >
                  <%= link_to "Journals", journals_path,
                    class: "text-purple-400 before:absolute before:inset-0" %>
                </li>
                <% if current_user.journals.any? %>
                  <% current_user.journals.each do |journal| %>
                    <li 
                      class="relative bg-slate-700 px-[16px] py-[8px] rounded-md"
                    >
                      <%= link_to journal.name, journal_notes_path(journal),
                        class: "text-purple-400 before:absolute before:inset-0" %>
                    </li>
                  <% end %>
                <% end %>
                <li 
                  class="bg-slate-700 rounded-md"
                >
                  <%= button_to "Sign out", destroy_user_session_path, 
                    method: :delete, data: {testid: "sign-out-btn"},
                    class: "h-full w-full text-left text-purple-400 px-[16px] py-[8px]" %>
                </li>
              <% else %>
                <li 
                  class="relative bg-slate-700 px-[16px] py-[8px] rounded-md"
                >
                  <%= link_to "Sign in", new_user_session_path,
                    class: "text-purple-400 before:absolute before:inset-0" %>
                </li>
                <li 
                  class="relative bg-slate-700 px-[16px] py-[8px] rounded-md"
                >
                  <%= link_to "Sign up", new_user_registration_path,
                    class: "text-purple-400 before:absolute before:inset-0" %>
                </li>
              <% end %>
            </ul>
            <div
              class="flex-none relative bg-slate-700 px-[16px] py-[8px] rounded-md text-base leading-4"
            >
              <%= link_to "Github", "https://github.com/mononoken/fae-scribe",
                class: "text-purple-400 before:absolute before:inset-0" %>
            </div>
          </div>
        </div>
      </nav>
    </header>
    <div class="flex-col overflow-y-auto">
      <% if flash.any? %>
        <div
          class="px-4 py-2 font-medium text-rose-400 bg-purplehaze"
          data-testid="flash-msg"
        >
          <% flash.each do |type, message| %>
            <% if message.is_a?(Array) %>
              <ul>
                <% message.each do |msg| %>
                  <li><%= msg %></li>
                <% end %>
              </ul> 
            <% else %>
              <div><%= message %></div>
            <% end %>
          <% end %>
        </div>
      <% end %>
      <main class="px-[16px]">
        <%= yield %>
      </main>
      <footer>
      </footer>
    </div>
  </body>
</html>

<%# Prepend is performed by Turbo Stream channel in controller %>

<%= turbo_stream.replace dom_id(@note, :new_counter) do %>
  <%= turbo_frame_tag dom_id(@note, :new_counter) do %>
    <%= render "form", {note: @note, counter: @counter} %>
  <% end %>
<% end %>

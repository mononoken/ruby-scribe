<div>
  <h1 class="font-bold text-4xl">New Journal</h1>

  <%= form_with model: @journal do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= form.submit "Create" %>
  <% end %>
</div>

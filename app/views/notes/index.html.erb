<div>
  <h1 class="font-bold text-4xl"><%= @journal.name %> Notes</h1>

  <%= render partial: "form", locals: {note: @note, journal: @journal} %>
  <ul>
    <% @notes.each do |note| %>
      <li><%= note.body %></li>
      <%= button_to 'Delete Note', note_path(note), method: :delete,
                                   data: { turbo_confirm: 'Are you sure?' } %>
    <% end %>
  </ul>
</div>

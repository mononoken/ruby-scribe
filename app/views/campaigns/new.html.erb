<div>
  <h1 class="font-bold text-4xl">New Campaign</h1>

  <%= form_with model: @campaign do |form| %>
    <%= form.label :name %>
    <%= form.text_field :name %>
    <%= form.submit "Create" %>
  <% end %>
</div>

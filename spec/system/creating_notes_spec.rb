require "rails_helper"

# RSpec.describe "creating notes", type: :system do
#   let(:note) { create(:note) }
#
#   context "when user submits valid note" do
#     before do
#     end
#
#     xit "shows newly created note on page" do
#       create_note
#
#       expect(page).to have_text(title_text)
#     end
#   end
#
#   context "when user submits empty note" do
#     xit "does not change notes shown on page"
#     xit "shows flash with empty note warning"
#   end
# end
